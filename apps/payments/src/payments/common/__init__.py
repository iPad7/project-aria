"""횡단 인프라 — 설정·엔진·브로커·로깅·ID.

**aria의 `common`과 성격이 다르다.** 저쪽은 컨텍스트들이 공유하는 커널(포트·이벤트
계약)이고, 여기는 그냥 레이어 밖 인프라다. payments는 컨텍스트가 하나뿐이라 공유할
커널이 없다 — 포트는 `application/port/out`에 산다.

**aria를 import하지 않는다.** 별도 서비스·별도 DB·별도 배포이고, 그것이 결제 시크릿을
격리하는 근거(NFR-SEC-1)다. 그래서 `ids`·`persistence` 같은 몇 줄이 양쪽에 겹치는데,
그 중복이 경계의 값이다 — 공유 라이브러리로 묶는 순간 두 서비스가 함께 배포된다.
"""
